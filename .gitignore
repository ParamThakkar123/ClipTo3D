# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.pytest_cache/

# Virtual environments
.venv

# Job outputs. The pipeline writes everything under a job root (see job_paths.py);
# `runs/` is the conventional place to put them.
runs/
frames
clips
audio
audio_frames
depth
depth_maps
dataset
gs_dataset
splat
cloud
export
gsplat_output/
point_clouds/*.ply
*.ply

# COLMAP databases and reconstructions (never commit these — a stale
# database.db used to be tracked and was a trap)
*.db
colmap/
structure_from_motion/colmap_output*/

# Model checkpoints
object_detection/sam2_checkpoints/
object_detection/sam2_config/
depth_estimation/depth_anything_ckpt/

# Opt-in SAM2 stage outputs
sam2_detections*.json
*_mask_*.png
*_processed.png
*_processed.jpg

# Secrets
.env
